1> what is a Spring Boot?
=> spring boot is a framework for building application in the java programming language.
or
- spring boot provid tools using this bild application.
or
- spring Boot makes it easy to create stand-alone, production - grade spring based application that you can just run.

2>spring:-
The core spring framework already reduces boilerplate code and provides a lot of helpful features for java applications.

-spring boot provide auto-configration
-standalone applications

advantage 
-without server configration depload spring boot application

//without spring boot, setting up a basic RESTful API reqyires menual configuration.

//controller 

//that was a spring boot code

@RestController
@RequestMapping("/api")
public class Mycontroller{
  @getMapping ("/hello")
  public String sayhello(){
    return "hello form Spring!";
  }
}

//Application Main
public class SpringApp{
  public static void main(String[] args){
    ApplicationContext context = new AnnotationConfigApplicationContext(MyController.class);
  }
}

// spring does simplify many tasks like

creating web applications,

working with databaese,

managing transaction

and more

//we need to manually set up the Spring application

auto-configuration

standalone Applications

//in olde worson first install tomcat server but this time not insatll onlie deploay jar file.




